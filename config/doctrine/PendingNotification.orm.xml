<?xml version="1.0" encoding="UTF-8"?>

<!--

 HiPay payment integration for Sylius

 (c) Hipay

 For the full copyright and license information, please view the LICENSE
 file that was distributed with this source code.

-->

<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:gedmo="http://gediminasm.org/schemas/orm/doctrine-extensions-mapping"
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                  http://doctrine-project.org/schemas/orm/doctrine-mapping.xsd"
>
    <entity name="HiPay\SyliusHiPayPlugin\Entity\PendingNotification"
            table="hipay_pending_notification"
            repository-class="HiPay\SyliusHiPayPlugin\Repository\PendingNotificationRepository"
    >
        <id name="id" column="id" type="bigint">
            <generator strategy="AUTO" />
        </id>

        <field name="eventId" column="event_id" type="string" length="64" unique="true" nullable="false" />
        <field name="transactionReference" column="transaction_reference" type="string" length="64" nullable="true" />
        <field name="status" column="status" type="integer" nullable="false" />
        <field name="priority" column="priority" type="smallint" nullable="false" />
        <field name="payload" column="payload" type="json" nullable="false" />
        <field name="state" column="state" type="string" length="16" nullable="false" enum-type="HiPay\SyliusHiPayPlugin\Webhook\PendingNotificationState" />
        <field name="attempts" column="attempts" type="smallint" nullable="false">
            <options>
                <option name="default">0</option>
            </options>
        </field>
        <field name="lastError" column="last_error" type="text" nullable="true" />
        <field name="availableAt" column="available_at" type="datetime" nullable="false" />
        <field name="claimedAt" column="claimed_at" type="datetime" nullable="true" />
        <field name="processedAt" column="processed_at" type="datetime" nullable="true" />

        <field name="createdAt" column="created_at" type="datetime">
            <gedmo:timestampable on="create"/>
        </field>
        <field name="updatedAt" column="updated_at" type="datetime" nullable="true">
            <gedmo:timestampable on="update"/>
        </field>

        <!--
             Composite index used by PendingNotificationRepository::claimBatch():
               WHERE state = :pending AND available_at <= :now
               ORDER BY priority ASC, id ASC
               FOR UPDATE SKIP LOCKED LIMIT :size
             The leading columns filter the working set; the trailing ones drive the
             strict FIFO-by-priority ordering.
         -->
        <indexes>
            <index name="idx_hipay_pending_claim" columns="state,available_at,priority,id" />
            <index name="idx_hipay_pending_tx_ref" columns="transaction_reference" />
        </indexes>
    </entity>
</doctrine-mapping>
