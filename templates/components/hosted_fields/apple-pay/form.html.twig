{# See components/hosted_fields/form.html.twig for the rationale behind
   data-live-ignore (preserves the SDK iframes across LiveComponent
   re-renders). #}
<div
    id="hipay-apple-pay-button"
    class="mt-4"
    data-live-ignore
    {{ stimulus_target('hipay-hosted-fields', 'placeholder') }}
></div>
